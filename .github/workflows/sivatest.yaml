name: testing some quick siva stuff
on:
  workflow_dispatch:
  push:
    branches:
      - main
env:
    Name: "Sivasobh"
jobs:
  job-1:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Hello from an inline bash script in a GitHub Action Workflow!"
      - run: echo "Hello ${{ env.Name }} from an inline bash script in a GitHub Action Workflow!"

  Job-2:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "This is second job"
      - run: echo "Hello ${{ env.Name }} from second job"
      - run: echo ${{ github.event_name }}
      - run: echo $HOSTNAME $PATH